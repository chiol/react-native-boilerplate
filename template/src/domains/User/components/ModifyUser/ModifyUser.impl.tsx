import React from "react";
import { View } from "react-native";
import TitleText from "common/components/TitleText";

const ModifyUser: React.FC = () => {
    return (
        <View>
            <TitleText title="modify global state" />
        </View>
    );
};

export default ModifyUser;
